DB_NAME=eshop
DB_USER=eshop
DB_PASSWORD=
SHOP_ADMIN_EMAIL=admin@example.com

# Redis AUTH — session store + login rate-limit counters live here
REDIS_PASSWORD=

GRAFANA_USER=admin
GRAFANA_PASSWORD=

# Dev UI tools (docker-compose.dev.yml only)
PGADMIN_EMAIL=admin@example.com
PGADMIN_PASSWORD=

# Mail — defaults to Mailpit (no changes needed for local dev)
# Uncomment and fill to switch to Gmail or Mailtrap:
# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USER=youraddress@gmail.com
# MAIL_PASSWORD=your-app-password
# MAIL_SMTP_AUTH=true
# MAIL_STARTTLS=true
